[sqlfluff]
# SQLFluff configuration for testing the schemachange templater
dialect = snowflake
templater = schemachange

# Common rule exclusions for development/testing
exclude_rules = references.keywords,structure.column_order

# Line length
max_line_length = 120

# Enable more detailed output
verbose = 0

[sqlfluff:templater:schemachange]
# Point to the test config file
config_folder = .
modules_folder = templates
vars = {"database_name": "MY_DATABASE", "schema_name": "ANALYTICS", "table_name": "customers", "state_filter": "CA", "start_date": "2023-01-01"}

[sqlfluff:indentation]
# Use consistent indentation
indented_joins = True
indented_using_on = True
template_blocks_indent = False

[sqlfluff:rules:capitalisation.keywords]
# Prefer lowercase for keywords (more modern style)
capitalisation_policy = lower

[sqlfluff:rules:capitalisation.identifiers]
# Keep identifiers lowercase
extended_capitalisation_policy = lower

[sqlfluff:rules:capitalisation.functions]
# Keep functions lowercase
extended_capitalisation_policy = lower

[sqlfluff:rules:layout.spacing]
# Standard spacing rules
single_table_references = consistent

[sqlfluff:rules:ambiguous.column_count]
# Allow SELECT * in examples
single_table_references = consistent
